# OceanScope Backend — Vercel Serverless Dependencies
#
# Minimum versions ensure compatibility with Vercel Python 3.9 runtime.
# Locally, newer versions from the .venv are used.

fastapi>=0.100.0
mangum>=0.17.0
netCDF4>=1.6.0
numpy>=1.24.0
pandas>=2.0.0
pydantic>=2.0.0
scipy>=1.10.0
uvicorn>=0.23.0
xarray>=2023.0.0
